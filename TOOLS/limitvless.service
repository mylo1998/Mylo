cat > /etc/systemd/system/limitvless.service << 'EOF'
[Unit]
Description=Xray VLESS IP Limit via API
After=network.target xray.service

[Service]
Type=simple
ExecStart=/bin/bash /etc/xray/limit.vless
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
EOF
